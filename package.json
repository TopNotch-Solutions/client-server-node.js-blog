{
  "name": "client-server",
  "version": "1.0.0",
  "description": "1. require('http'); used to make requests to the server. First require the module, then create the server \r     then finally listen for incomming requests on the port specified which in this case is the localhost:3000.\r 2. Install nodemon from npmjs.com // helps with restarting the server once you make any changes.\r 3.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.9",
    "express": "^4.18.3",
    "lodash": "^4.17.21",
    "mongodb": "^6.4.0",
    "mongoose": "^8.2.1"
  }
}
